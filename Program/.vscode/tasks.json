{
    // See https://go.microsoft.com/fwlink/?LinkId=733558
    // for the documentation about the tasks.json format
    "version": "2.0.0",
    "tasks": [
        {
            "label": "My Build",
            "group": "build",
            "type": "shell",
            "command": "g++ -o main.exe main.cpp",
        },
        {
            "label": "min32-build",
            "group": "build",
            "type": "shell",
            "command": "mingw32-make makefile main.exe clean",
        }
    ]
}